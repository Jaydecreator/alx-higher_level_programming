-- listing all records from table "second_table"
-- we will list the only two columns in descending order from top  to bottom according to score
SELECT score, name FROM second_table ORDER BY score DESC;
